{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "provider-clickhouse.fullname" . }}
  labels:
    {{- include "provider-clickhouse.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "provider-clickhouse.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "provider-clickhouse.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
